{
    "name": "IBBJS",
    "type": "module",
    "outDir": "dest",
    "devDependencies": {
        "@babel/core": "^7.21.8",
        "@babel/plugin-transform-modules-commonjs": "^7.21.2",
        "@babel/preset-env": "^7.21.5",
        "@babel/preset-typescript": "^7.21.5",
        "@babel/types": "^7.21.4",
        "@jest/globals": "^29.5.0",
        "@types/jest": "^29.5.1",
        "@typescript-eslint/eslint-plugin": "^5.59.2",
        "@typescript-eslint/parser": "^5.59.2",
        "babel-jest": "^29.5.0",
        "eslint": "^8.39.0",
        "jest": "^29.5.0",
        "pretty-format": "^27.0.2",
        "tsc-alias": "^1.8.5"
    },
    "scripts": {
        "build": "tsc && npm run build:copy && npm run build:fiximports && npm run build:revision",
        "test": "jest --silent=false",
        "test-coverage": "jest --silent=false --collect-coverage",
        "cleanbuild": "npm run clean && npm run build",
        "clean": "rm -rf dest",
        "publish": "npm run cleanbuild && npm run publish:script",
        "publish:script": "sh bin/publish.sh Commit $(git rev-parse HEAD) to $(git config --get remote.origin.url)",
        "build:fiximports": "sh bin/fiximports.sh",
        "build:copy": "mkdir -p dest && (cd public; tar cf - .) | (cd dest; tar xf -)",
        "build:revision": "sh bin/getrevision.sh"
    },
    "babel": {
        "env": {
            "test": {
                "plugins": [
                    "@babel/plugin-transform-modules-commonjs"
                ]
            }
        }
    }
}
